import { Section, Aside } from '../../shared';

function EarnContainer() {
  return (
    <Section className={`h-screen overflow-hidden w-[100%] bg-gray`}>
      <Aside>
        <div className="text-white">Report and earn</div>
      </Aside>
    </Section>
  );
}

export default EarnContainer;
